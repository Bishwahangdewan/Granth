/**
 * lodash-like mapValues function
 * Returns new object with same keys
 * and each value equal to result of running it through provided callback
 */
export declare function mapValues<T extends {
    [key: string]: any;
}, R>(callback: (originalValue: any) => R, obj: T): {
    [B in keyof T]: R;
};
/**
 * lodash-like mapValues function
 * Returns key of value matching search input
 */
export declare function findKey<T extends {
    [key: string]: any;
}, K extends keyof T>(value: any, obj: T): K | undefined;
/**
 * Just a throttle function
 */
export declare function throttle(delay: number, fn: (...args: any[]) => any): (...args: any[]) => any;
/**
 * Helper for calculating top and bottom position of element within the screen
 * based on result of getBoundingClientRect
 */
export declare const getComponentBounds: (windowHeight: number) => (rect: DOMRect) => {
    top: number;
    bottom: number;
};
